version: "3.9"

services:
  api:
    build: .
    container_name: rate-limited-api
    ports:
      - "8000:8000"
    volumes:
      - rate_limit_data:/app/state
    env_file:
      - .env

volumes:
  rate_limit_data:
